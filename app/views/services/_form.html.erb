<%= form_for(@service, class: 'form-inline') do |f| %>
  <% if @service.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service.errors.count, "error") %> prohibited this service from being saved:</h2>

      <ul>
      <% @service.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :address %>
    <%= f.text_field :address, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :port %>
    <%= f.number_field :port, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :position %>
    <%= f.number_field :position, class: 'form-control', value: @position %>
  </div>
  <div class="form-action">
    <%= button_tag ' Submit', class: 'btn btn-primary fa fa-check' %>
    <%= link_to ' Back', services_path, class: 'btn btn-default fa fa-reply' %>
    <%= link_to ' Delete', @service, method: :delete, class: 'delete btn btn-primary btn-danger fa fa-trash-o', data: { confirm: 'Are you sure?' } if @service.id? %>
  </div>
<% end %>
